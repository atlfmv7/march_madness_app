# pytest.ini
# -------------------------------
# Make pytest treat the project root (.) as part of sys.path
# so "from app import create_app" works during tests.
# -------------------------------
[pytest]
pythonpath = .
